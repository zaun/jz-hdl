// SYNC_RESET_ACTIVE_INVALID: SYNCHRONOUS block RESET_ACTIVE must be High or Low.
// Verified

@project SYNC_RESET_ACTIVE_INVALID_PROJECT
  @top SYNC_RESET_ACTIVE_INVALID_EXAMPLE {
    IN  [1] clk = _;
    IN  [1] rst = _;
    OUT [8] out = _;
  }
@endproj

@module SYNC_RESET_ACTIVE_INVALID_EXAMPLE
  PORT {
    IN  [1] clk;
    IN  [1] rst;
    OUT [8] out;
  }

  REGISTER {
    r [8] = 8'h00;
  }

  ASYNCHRONOUS {
    out <= r;
  }

  SYNCHRONOUS(CLK=clk RESET=rst RESET_ACTIVE=Maybe) {
    r <= 8'h01;
  }
@endmod
