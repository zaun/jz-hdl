// LIT_VALUE_INVALID: lit() value must be nonnegative.
// Verified

@project LIT_VALUE_INVALID_PROJECT
  @top LIT_VALUE_INVALID_EXAMPLE {
    OUT [1] data = _;
  }
@endproj

@module LIT_VALUE_INVALID_EXAMPLE
  PORT {
    OUT [1] data;
  }

  ASYNCHRONOUS {
    data <= lit(4, -1);
  }
@endmod
