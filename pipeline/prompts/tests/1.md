**Role:** You are a Senior SDET specializing in compiler validation for a hardware description language.

## Context:
I am building a compiler for JZ-HDL, a hardware correctness language. The compiler enforces strict semantic rules at compile time. I need a comprehensive test plan that ensures every rule the compiler checks is validated.

There are two key documents:
1. **The Language Specification** (`specification/jz-hdl-specification.md`): The source of truth for all language requirements, semantics, and constraints.
2. **The Rule ID Registry** (`compiler/src/rules.c`): Contains every concrete diagnostic rule ID the compiler implements (e.g., `CLOCK_GEN_MISSING_INPUT`, `BUS_SIGNAL_UNDEFINED`). This is the completeness checklist.

## CRITICAL CONSTRAINTS — files you must NEVER read, search, or reference:
- `compiler/tests/validation_old/` or any directory containing "old"
- `compiler/tests/issues.md`
- `compiler/tests/not_tested.md`
- `compiler/tests/summary.md`
- `compiler/tests/summary-old.md`

These files contain stale or potentially incorrect assessments from previous test runs. Your test plans must be derived exclusively from the specification and `rules.c`. Do not let prior judgments about whether a rule "works" or is "dead" influence your plans — plan a test for every rule and let the test execution phase discover any compiler bugs.

## Task:
Create/update a comprehensive **Test Plan** for every individual subsection identified in the Language Specification.

### Documents to read:
- `specification/jz-hdl-specification.md` (primary — drives test plan structure and scenarios)
- `compiler/src/rules.c` (completeness checklist — every rule ID must be accounted for)
- `compiler/tests/validation/` (existing test baseline — do not duplicate)
- `pipeline/test_*.md` (existing test plans — update in place)

### Step 1: Review existing state.
- Review existing test plans in `pipeline/test_*.md`. Update them in place — do not delete and recreate. If a plan already covers a spec subsection, add missing rules and scenarios to it rather than starting from scratch. Only create new plan files for spec subsections that have no existing plan.
- Review existing tests in `compiler/tests/validation/` to understand the current baseline and avoid duplicating work.

### Step 2: Build the master rule list.
Delete existing `pipeline/rule_coverage.md`.
Read `compiler/src/rules.c` and extract every rule ID into a flat list. Count them. Write this count down — it is your target.

### Step 3: Build the coverage map.
Write a file `pipeline/rule_coverage.md` containing a markdown table with one row per rule ID from Step 2:

```
| Rule ID | Test Plan File | Status |
|---------|---------------|--------|
| CLOCK_GEN_MISSING_INPUT | test_6_4-clocks_block.md | Tested |
| BUS_PORT_UNKNOWN_BUS | test_6_8-bus_aggregation.md | Not Tested |
| IO_BACKEND | — | Not testable: backend-only, not reachable via --lint |
```

**Status** must be one of:
- **Tested** — rule appears in a test plan's "Rules Tested" section (must name the file)
- **Not Tested** — rule does **not** appear in a test plan's "Rules Tested" section (must name the file)
- **Not Testable: `<reason>`** — rule cannot be tested via `--info --lint` (must give specific reason)

**Every rule ID from Step 2 must appear in this table.** No exceptions, no omissions.

### Step 4: Verify completeness.
Count the rows in `pipeline/rule_coverage.md`. This count MUST equal the count from Step 2. If it does not, you have missed rules — go back and find them. **This count MUST equal the count from Step 2 before moving forward**

### Step 5: Update/create test plans (guided by the coverage map).
For any rule ID with Status "Not Tested" in `pipeline/rule_coverage.md`, add a test scenario to the named test plan file now. For any rule ID you cannot map to a spec subsection, add it to the closest related test plan — do not skip it.

### Step 6: Reconcile "Rules Not Tested" sections against existing validation tests.
After updating test plans, each plan's "Rules Not Tested" section (5.2) may contain stale entries — rules listed as untested that actually have validation tests (either in this section or cross-referenced from other sections). Fix this:

1. **Scan for existing tests.** For every rule in a plan's "Rules Not Tested" section, glob `compiler/tests/validation/` for `.jz` files whose name contains that rule ID. Check ALL sections, not just the plan's own section prefix.
2. **Move tested rules.** If a validation test exists for a rule listed in "Rules Not Tested", move it to "Rules Tested" (5.1) with the test file name(s). If the test lives under a different section prefix, note the cross-reference (e.g., `4_1_DIRECTIVE_INVALID_CONTEXT-nested_structural_directives.jz (section 4.1)`).
3. **Clean up stale entries.** After moving tested rules, every remaining entry in "Rules Not Tested" must have a concrete, still-valid reason (e.g., "backend-only", "fires at simulation runtime", "requires chip-specific infrastructure"). Remove placeholder rows that use `--` or `(none)` as the rule ID — replace with "All rules for this section are tested." if the section is empty.
4. **Check for new rules.** If any rule ID in `rules.c` maps to this plan's spec section (based on the `S<section>` reference in the rule's description) but is missing from both "Rules Tested" and "Rules Not Tested", add it to the appropriate section.

### Test Framework:
Each validation test consists of a paired `.jz` and `.out` file:
- The `.jz` file contains JZ-HDL source code designed to trigger specific diagnostics.
- The `.out` file contains the expected diagnostic output from running `compiler/build/jz-hdl --info --lint <file>.jz`.
- **Happy-path tests** use an empty `.out` file (no diagnostics expected) to verify that valid code compiles cleanly without false positives.
- Tests should include both error cases AND valid negative cases (code that should NOT trigger the rule) in the same file where practical.
- Tests targeting chip-specific rules (e.g., resource limits, pin standards) must include full project infrastructure (`CHIP=`, `IN_PINS`, `OUT_PINS`, `MAP`, `CLOCKS` blocks).

### Test Plan Structure:
For each spec subsection, provide:

1.  **Objective:** What specific language requirement is being verified?
2.  **Test Scenarios:**
    *   **Happy Path:** Valid code that must compile without diagnostics (empty `.out`). These are regression tests ensuring the compiler does not reject valid constructs.
    *   **Error Cases:** Code that violates the spec and must produce specific diagnostics.
    *   **Edge Cases:** Boundary conditions, unusual but valid constructs, scope interactions across module hierarchies.
3.  **Input/Output Matrix:**
    *   For each test scenario: the triggering construct, the expected rule ID, and the expected diagnostic severity (error/warning/info).
4.  **Rules Matrix:**
    *   **Rules Tested:** Every rule ID from `rules.c` that maps to this spec subsection, with at least one test scenario per rule.
    *   **Rules Not Tested:** Any rule ID that cannot be tested in the `--info --lint` framework, with a documented reason (e.g., fires during backend only, dead code, requires simulation).

### Formatting:
Format the output in Markdown. Use headers for each subsection and tables for the Input/Output and Rules matrices.

### Output Location:
File name should be lowercase and use underscores for spaces.
```
pipeline/test_<section>_<subsection>-<plan_name>.md
pipeline/test_<section>_<subsection>_<subsubsection>-<plan_name>.md
```
